import styled from "styled-components";

export const Title = styled.h1`
    color: var(--color-primary);
    font-weight: 700;
    font-size: 26px;

    text-align: center;
    margin-bottom: 36px;
`